import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
	positionAbsolute: { position: 'absolute' },
	positionRelative: { position: 'relative' },
});

export const { positionAbsolute, positionRelative } = styles;
